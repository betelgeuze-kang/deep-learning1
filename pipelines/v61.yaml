{
  "schema_version": "pipeline.v1",
  "pipeline_id": "v61-checkpoint-runtime-core",
  "description": "Adapter inventory for SSD-resident MoE runtime readiness and one-token path work.",
  "claim_boundary": "Runtime contract and bounded local parity evidence only; real model inference remains blocked until a real checkpoint path closes one-token logits parity.",
  "stages": [
    {
      "stage_id": "v61i-typed-100b-moe-readiness",
      "requires": [],
      "adapter": "shell",
      "runtime_overrides": ["V61I_REUSE_EXISTING"],
      "command": ["./experiments/test_v61i_100b_moe_active_sparse_run.sh"],
      "inputs": ["experiments/run_v61i_100b_moe_active_sparse_run.sh"],
      "outputs": ["results/v61i_100b_moe_active_sparse_run_summary.csv"],
      "ready_fields": ["logical_100b_contract_fixture_ready", "real_100b_inference_ready"],
      "blocked_fields": ["real_100b_inference_ready", "release_ready"],
      "typed_readiness": {
        "contract_ready": "contract_ready",
        "fixture_execution_ready": "fixture_execution_ready",
        "real_model_execution_ready": "real_model_execution_ready",
        "heldout_metric_ready": "heldout_metric_ready",
        "human_review_ready": "human_review_ready",
        "independent_reproduction_ready": "independent_reproduction_ready",
        "release_ready": "release_ready"
      },
      "model_visible_inputs": [],
      "model_visible_input_checks": [],
      "evaluator_only_fields": ["checkpoint_hash", "tensor_page_hash", "runtime_measurement"],
      "evidence_boundary": "Contract/fixture execution only; real_100b_inference_ready=0.",
      "claim_boundary": "Logical fixture readiness is not real 100B inference."
    },
    {
      "stage_id": "v61aa-hotset-tensor-slice-verifier",
      "requires": [],
      "adapter": "shell",
      "runtime_overrides": ["V61AA_REUSE_EXISTING"],
      "command": ["./experiments/test_v61aa_hotset_tensor_slice_verifier.sh"],
      "inputs": [
        "results/v61z_hotset_direct_io_replay_summary.csv",
        "results/v61v_remote_page_tensor_binding_summary.csv",
        "results/v61y_hotset_local_materialization_verifier_summary.csv"
      ],
      "outputs": ["results/v61aa_hotset_tensor_slice_verifier_summary.csv"],
      "ready_fields": ["v61aa_hotset_tensor_slice_verifier_ready", "bf16_tensor_slice_stats_ready"],
      "blocked_fields": ["full_checkpoint_materialization_ready", "full_safetensors_page_hash_binding_ready", "actual_model_generation_ready", "real_release_package_ready"],
      "typed_readiness": {
        "contract_ready": "v61aa_hotset_tensor_slice_verifier_ready",
        "fixture_execution_ready": "bf16_tensor_slice_stats_ready",
        "real_model_execution_ready": "actual_model_generation_ready",
        "heldout_metric_ready": "0",
        "human_review_ready": "0",
        "independent_reproduction_ready": "0",
        "release_ready": "real_release_package_ready"
      },
      "model_visible_inputs": [],
      "model_visible_input_checks": [],
      "evaluator_only_fields": ["remote_page_sha256", "tensor_segment_bytes", "sampled_bf16_values"],
      "evidence_boundary": "Sampled BF16 tensor-slice/stat evidence over bounded real checkpoint hotset pages; no generation or full checkpoint materialization.",
      "claim_boundary": "Tensor-slice verifier only; not one-token logits parity or real model inference."
    },
    {
      "stage_id": "v61ab-hotset-tensor-tile-parity",
      "requires": ["v61aa-hotset-tensor-slice-verifier"],
      "adapter": "shell",
      "runtime_overrides": ["V61AB_REUSE_EXISTING", "V61AB_SAFETENSORS_ROOT"],
      "command": ["./experiments/test_v61ab_hotset_tensor_tile_quant_probe.sh"],
      "inputs": ["results/v61aa_hotset_tensor_slice_verifier_summary.csv"],
      "outputs": ["results/v61ab_hotset_tensor_tile_quant_probe_summary.csv"],
      "ready_fields": ["torch_matvec_parity_ready", "expert_ffn_parity_contract_ready"],
      "blocked_fields": ["expert_ffn_parity_real_model_execution_ready", "real_model_execution_ready"],
      "typed_readiness": {
        "contract_ready": "expert_ffn_parity_contract_ready",
        "fixture_execution_ready": "torch_matvec_parity_ready",
        "real_model_execution_ready": "expert_ffn_parity_real_model_execution_ready",
        "heldout_metric_ready": "0",
        "human_review_ready": "0",
        "independent_reproduction_ready": "0",
        "release_ready": "real_release_package_ready"
      },
      "model_visible_inputs": [],
      "model_visible_input_checks": [],
      "evaluator_only_fields": ["checkpoint_tensor_page", "torch_reference_output", "quant_error"],
      "evidence_boundary": "Bounded hotset tile parity and optional local checkpoint FFN contract; no logits parity.",
      "claim_boundary": "Bounded tile parity only; no one-token logits parity claim."
    }
  ]
}
